[project]
name = "fundforge-collector"
version = "0.1.0"
description = "FundForge 数据采集服务 — akshare 数据源封装"
requires-python = ">=3.11"
dependencies = [
    "akshare>=1.15.0",
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "pandas>=2.0.0",
]
